apiVersion: v1
kind: Service
metadata:
  name: energy-router
  labels:
    app: energy-router
spec:
  type: ClusterIP
  selector:
    app: energy-router
  ports:
    - protocol: TCP
      port: 8009
      targetPort: http
      name: http
